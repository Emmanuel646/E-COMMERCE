{% extends 'base.html.twig' %}


{% block body %}

    <div class="container">
        
        <section class="row">
            {% for product in products %}
                {% if product.category == 'title-led' %}
                    <div class="col-12" style="height:70px; margin: 5px">
                        <h2 class="d-flex justify-content-center p-1">{{ product.name }}</h2>
                    </div>
                    <div class="row title-image mb-5">
                        <img src="../assets/images/ecranlcd.jpg">
                    </div>
                {% elseif product.category == 'ecran-led' %}
                    <div class="col-6 mb-3">
                        <div class="card h-100">
                            <div class="card-title">
                                <h3 class="card-title text-center">{{ product.name }}</h3>
                                <div class="card-body text-center">
                                    <p>Ce clavier est composé de {{ product.touch }} touches au format {{ product.format }}, convenant à de multiples utilisations.</p>
                                    <p>Prix TTC est de : {{ product.price }} €</p>
                                    {#<a href="#" class="btn btn-primary">Ajouter au panier : <br> {{ product.name }}</a>#}
                                </div>
                            </div>
                        </div>
                    </div>
                {% endif %}
            {% endfor %}
        </section>
        <section class="row">
            {% for product in products %}
                {% if product.category == 'title-oled' %}
                    <div class="col-12" style="height:70px; margin: 5px">
                        <h2 class="d-flex justify-content-center p-1">{{ product.name }}</h2>
                    </div>
                {% elseif product.category == 'ecran-oled' %}
                    <div class="col-6 mb-3">
                        <div class="card h-100">
                            <div class="card-title">
                                <h3 class="card-title text-center">{{ product.name }}</h3>
                                <div class="card-body text-center">
                                    <p>Ce clavier est composé de {{ product.touch }} touches au format {{ product.format }}, convenant à de multiples utilisations.</p>
                                    <p>Prix TTC est de : {{ product.price }} €</p>
                                    {#<a href="#" class="btn btn-primary">Ajouter au panier : <br> {{ product.name }}</a>#}
                                </div>
                            </div>
                        </div>
                    </div>
                {% endif %}
            {% endfor %}
        </section>
    </div>

{% endblock %}